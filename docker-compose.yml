services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - APP_VERSION=1.0.0

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
